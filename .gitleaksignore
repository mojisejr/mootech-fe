# .gitleaksignore — ยกเว้นเป็นราย "ลายนิ้วมือ" เท่านั้น (commit:file:rule:line)
#
# 🔴 กติกาของไฟล์นี้: ยกเว้นได้เฉพาะค่าที่ **พิสูจน์ได้ว่าไม่ใช่ของจริง** และต้องเขียนไว้ว่าทำไม
#    ❌ ห้ามยกเว้นทั้งไฟล์/ทั้งโฟลเดอร์ — นั่นทำให้ที่นั่นกลายเป็นจุดบอดถาวรของตัวสแกน
#       วันหน้าถ้ามีคนเผลอวางคีย์จริงลงไฟล์เดียวกัน จะไม่มีใครเห็น
#    ลายนิ้วมือผูกกับ commit เดียว ⇒ commit ใหม่ในไฟล์เดิมยังถูกสแกนตามปกติ
#
# ── #231 / PR #232 ──────────────────────────────────────────────────────────────
# testenv/scripts/guard.test.sh บรรทัด 68-69 ของ commit 73f042c
# ค่าที่ถูกจับ: SUPABASE_SERVICE_ROLE_KEY=sb_secret_AbCdEf123456
#              SUPABASE_ANON_KEY=sb_publishable_AbCdEf123456
# ทั้งคู่เป็นค่าที่ "AbCdEf123456" ต่อท้าย prefix — พิมพ์ขึ้นมาเองเพื่อทดสอบว่า guard.sh จับ *รูปแบบ*
# คีย์ Supabase รุ่นใหม่ได้ (ตู๋เจอตอน verify ว่ากฎเดิม ^eyJ เฝ้าแค่รุ่นเก่า) ⇒ มันต้องหน้าตาเหมือนของจริง
# ไม่งั้นเทสต์ทดสอบอะไรไม่ได้เลย ⇒ ด่านสองด่านทำงานถูกทั้งคู่แล้วชนกันเอง
# ไม่มีคีย์จริงเกี่ยวข้อง · commit ถัดไปประกอบสตริงตอนรันแล้ว จึงไม่เกิดซ้ำในอนาคต
73f042c1469967baf2ce6afec8ea4a6609ccf846:testenv/scripts/guard.test.sh:generic-api-key:68
73f042c1469967baf2ce6afec8ea4a6609ccf846:testenv/scripts/guard.test.sh:generic-api-key:69
