# avatar เลิกโชว์ "F" — รูปจริง → มาสคอต (ทาง ข ของฟีม)

**มุน 2026-08-06** · branch `fix/avatar-mascot-fallback` @ `e3fd355` · base `bcfadcf`
**สภาพที่วัด** (จำเป็นต่อการอ่านผล — ดูข้อ 7): dev server `next dev :3131` · ❌ ไม่มี backend · stub `/api/user` + `/api/v2/calendar-month` + `/api/v2/day-detail` · Playwright 393×852 dsf3 · reducedMotion

## ภาพ route จริง @393 ก่อน/หลัง
`harness/out/av-before-*.png` · `av-after-*.png` — 5 หน้า + 2 สถานะรูป

## 🧊 COLD LOAD — **context ใหม่ทุกหน้า ไม่นำทางในแอป** (รอบแก้ของ goo `4938f4c`)
```
มี cookie รูปจริง        first paint      →  settled
  home-preview           (ยังไม่มี)          รูปจริง 01.png
  calendar               มาสคอต             รูปจริง 01.png
  day                    (ยังไม่มี)          รูปจริง 01.png
  notifications          มาสคอต             รูปจริง 01.png
  service                มาสคอต             รูปจริง 01.png
ไม่มี cookie รูปจริง       settled: มาสคอต ค้างไว้  ❌ ไม่ใช่ตัวอักษร ❌ ไม่ใช่ช่องว่าง
❌ ไม่มี "F" ในทุกจังหวะ ทั้ง first paint และ settled
```
**ลำดับที่เห็นบนจอจริง** (วัดทุกเฟรมตั้งแต่ก่อน navigation):
```
287ms  มาสคอต 01-nav.png
635ms  รูปจริง 01.png          ⇒ เห็นมาสคอตราว 350 ms แล้วสลับ 1 ครั้ง
```

### 🔬 การสลับ **ไม่ทำให้ของเลื่อน** — พิสูจน์ด้วย control ไม่ใช่ด้วยเหตุผล
```
มีการสลับ (มี cookie รูป)     CLS 0.0234
ไม่มีการสลับ (ไม่มี cookie)    CLS 0.0234    ← เท่ากันเป๊ะ
```
⇒ **ตัวเลขที่เหลือมาจากที่อื่นบนหน้าปฏิทิน ไม่ใช่จาก avatar** (วงกลม 40px ขนาดคงที่ สลับภาพในกล่องเดิม)
⇒ ถ้าอ้างแค่ *"วงกลมขนาดคงที่ก็ไม่เลื่อนสิ"* คือการให้เหตุผล **control คือสิ่งที่ทำให้มันเป็นการวัด**
⚠️ **การกระพริบมีจริง** (มาสคอต→รูป 1 ครั้ง) — goo แจ้งไว้ **รอฟีมดูของจริงแล้วเคาะ ยังไม่ถือว่ารับแล้ว**

## ผลดิบ — **บั๊กอยู่ในภาพ "ก่อน" ชัดๆ**
```
                 ก่อน                       หลัง
home-preview     letter "ม"                 มาสคอต painted
calendar         letter "F"   🔴            มาสคอต painted
day              letter "F"   🔴            มาสคอต painted
notifications    letter "F"   🔴            มาสคอต painted
service          letter "F"   🔴            มาสคอต painted
photo-ok         รูปจริง 01.png              รูปจริง 01.png      ← home ไม่ regress
photo-404        letter 🔴                  มาสคอต painted      ← 404 ตกมาที่มาสคอต ไม่ใช่รูปแตก
```
**❌ ไม่มี "F" เหลือที่ไหนเลย ทุกสถานะ** · `avatar-letter` ถูกลบทั้ง testid และตัวอักษร

## 🔴 การยิงเน็ตเพิ่ม = 0
`01-nav.png` (63 KB) **ถูกโหลดอยู่แล้วโดย `MateAIButton`** ⇒ ไม่มีคำขอใหม่เกิดขึ้นเพราะ avatar
เลือก `01-nav` ไม่ใช่ `01.png` เพราะ **มันคืออาร์ตเดียวกันคนละความละเอียด** — `01.png` = 1.4 MB (22 เท่า) สำหรับวงกลม 40px ที่มองไม่เห็นความต่าง

## proof-of-teeth
**ไม่เพิ่มด่านใหม่** ตามคำสั่งฟีม · แต่ 2 ด่านเดิมยึด `avatar-letter` อยู่ ⇒ **retarget ไม่ใช่ลบ**

| ด่าน | เดิมถาม | ตอนนี้ถาม | สถานะ |
|---|---|---|---|
| `run-shared-topbar` #3 | "fallback เป็น **ตัวอักษร**" | "fallback เป็น**มาสคอต** และ**ไม่มีตัวอักษร**" | ✅ ผ่าน (ดูข้อ 7 เรื่องสภาพ) |
| `run-header-structure` | "ไม่มีรูป → letter 1 ตัว" | "ไม่มีรูป → มาสคอต 1 ตัว" | ⚠️ **แก้แล้วแต่รันไม่ได้** (ดูข้อ 3) |

🔴 **และด่าน `run-shared-topbar` จับบั๊กจริงของผมได้ 1 ตัว** — ดูข้อ 1

ANCHOR: harness/capture-coming-soon.ts#coming-soon-toast

## adversary sign-off

1. **🔴 ผมทำ SSR พังเอง และด่านเก่าเป็นคนจับ** — `useMemberIdentity()` เรียก `useCookies` ⇒ **โยน `Missing <CookiesProvider>`** เมื่อ `TopBarAvatar` ถูกเรนเดอร์เดี่ยวๆ ผ่าน `renderToStaticMarkup`
   **พิสูจน์ว่าเป็นของผม**: stash แล้วรันบนกิ่งสะอาด ⇒ **ด่านนี้เขียว** ⇒ **ผมทำพัง ไม่ใช่ของเดิม**
   ⇒ แก้โดยให้การเรนเดอร์เดี่ยว**ห่อ `CookiesProvider` เหมือนที่ `_app.tsx` ห่อทุกหน้า** — ทำให้ฮาร์เนสเหมือน production ไม่ใช่ทดสอบสภาพที่ผู้ใช้ไม่มีวันเจอ
   ⚠️ **ข้อจำกัดที่ตามมา**: `TopBarAvatar` **ต้องมี `CookiesProvider` ถึงจะเรนเดอร์ได้** ทุกหน้าใน app มีอยู่แล้ว (`_app.tsx` ห่อ `<Component>`) แต่การเรนเดอร์นอก app จะ throw
   ⇒ **ผมเคยจดข้อนี้เป็น "หนี้ที่ต้องแก้" — บองแก้ให้ และเขาถูก**: การให้ hook คืนค่าว่างเงียบๆ แทน throw คือ **การเปลี่ยนของดังให้เงียบ** ซึ่งเป็นตระกูลเดียวกับที่เราไล่ฆ่ากันมาทั้งวัน
   ⇒ **การ throw คือพฤติกรรมที่ถูกต้อง** — component ที่ต้องการ provider แล้วตะโกนเมื่อไม่มี ซื่อสัตย์กว่าตัวที่แกล้งทำงานต่อ
   ⇒ **ไม่ใช่หนี้ · เป็นสัญญาที่บันทึกไว้** ⇒ ❌ ห้ามใครมา "ซ่อม" ให้มันเงียบ

2. **🔴 ค่าครอบที่วัดนอก component ใช้ไม่ได้ตรงๆ** — prototype ให้ `-30%` แต่ในของจริง `absolute` ไม่มี `top/left` ⇒ ยึดตำแหน่ง static ในพาเรนต์ที่ `place-items-center` ⇒ **หัวโดนตัด**
   ⇒ ปัก `left-0 top-0` แล้ว**วัดใหม่ในของจริง**: ลอง −30 / −18 / −12 **ดูทั้งสามที่ขนาดจริง** ⇒ −18 ได้ตา ปาก และหมวกครบ
   ⇒ **ค่าที่วัดในกล่องทดลอง ไม่ใช่ค่าที่ใช้ได้ในกล่องจริง** — ต้องวัดซ้ำที่ปลายทางเสมอ

3. **`run-header-structure` แก้แล้วแต่รันไม่ได้ — และผมไม่อ้างว่าทดสอบแล้ว**
   มัน timeout ที่ `greeting-name` **ทั้งบนกิ่งผมและกิ่งสะอาด** ⇒ **พังอยู่ก่อน ไม่ใช่รัศมีระเบิดผม** ⇒ ไม่ซ่อมในใบนี้
   ⇒ แก้ assertion แบบกลไกให้ตรงกับตัวที่รันได้ (`run-shared-topbar`) + **เขียนกำกับในไฟล์ว่ายังไม่ได้ทดสอบ**

4. **ถอน `enforced_by` 3 entry** (บองสั่ง) — `greeting-name-truncation` · `home-bell-fullpage-not-modal` · `shared-topbar-extract-no-consumer-drift`
   ⇒ ต่อ CI หรือถอนคำโฆษณา **เลือกอย่างใดอย่างหนึ่ง** · ใบนี้ไม่ต่อ CI (ยกไปก้อนจัดบ้านด่าน) ⇒ **ถอน**

5. **`avatar-static` คงไว้ตามที่รับปาก** — `capture-coming-soon` **อยู่ใน CI** ⇒ ถ้าหายคือ CI แดง ⇒ **รันแล้ว 22/22 ผ่าน**

7. **🔬 `run-shared-topbar` ให้ผลคนละแบบตามสภาพแวดล้อม — บันทึกทั้งสองสภาพ ไม่เลือกอันที่ดูดี**
   ผมรันเอง 2 สภาพ (❌ ไม่ลอกผลตู๋มาแปะ เพราะนั่นคือเขียนสิ่งที่ตัวเองไม่ได้เห็น):
   ```
   A · dev server UP        6 มาร์กอัป ✓ · เบราว์เซอร์รัน · ✗ 1 (calendar bell — ต้องมี backend) → FAIL(1)
   B · ไม่มี dev server      6 มาร์กอัป ✓ · แล้ว CRASH ที่ ERR_CONNECTION_REFUSED → ไม่มีคำตัดสินเลย
   ตู๋เห็น                    timeout ที่ greeting-name  ⇒ สภาพที่ 3 ที่ผมยังไม่ได้ทำซ้ำ
   ```
   ⇒ **ส่วนมาร์กอัป (สิ่งที่ใบนี้แก้) ผ่านทุกสภาพ** · ส่วนเบราว์เซอร์ขึ้นกับว่ามีอะไรเป็นอยู่รอบตัว
   ⇒ **นี่ไม่ใช่แค่เรื่องต้องเคลียร์ มันคือ finding** (บอง): ด่านที่ตอบไม่เหมือนกันเมื่อ dev server อยู่/ไม่อยู่ **กำลังวัดสภาพแวดล้อมปนกับวัดโค้ด** ⇒ คนที่รันทีหลังจะอ่านผลผิดโดยไม่รู้ตัว
   ⇒ **จึงบันทึกสภาพกำกับไว้ที่หัวเอกสาร** และไม่เขียนว่า "ผ่าน" ลอยๆ

8. **ยังไม่ครอบ (A2)** — ทดสอบผ่าน `home-preview` ไม่ใช่ `/v2` ตัวจริง (ต้องมี session) · ไม่ได้ทดสอบ cookie จริงจาก LINE/Google login (ใช้ prop + 404 แทน) · ไม่ได้วัด CLS เป็นตัวเลข
