# EYE PROOF — ปฏิทินดวง: แถบเลือกวัน + ตารางเดือนให้ตรง Figma (และ 干支 ที่ไม่เคยขึ้นเลย)

**Anchor:** `harness/run-calendar-fidelity.ts` (17/17)
**PR:** feat/v2-calendar-fidelity · base = main (`b29f7c5`)
**Ledger:** `harness/bug-ledger/` → `calendar-fidelity`
**Plan:** ❄️ FROZEN FINAL — `lamun-oracle/ψ/plans/2026-08-03_FROZEN-calendar-arc-zones-1-3.md` (Zone 3.1 + 3.2)

ANCHOR: harness/run-calendar-fidelity.ts#mut-grade-letter-back

## drift ที่เจอเมื่อเทียบกับ node จริง (`get_design_context` 368:9832)

| | Figma | main |
|---|---|---|
| ช่องวัน | `[เลขวัน Bold 13 #0B305B] [干支 Regular 8 #1455A4]` / `[% Bold 12 สี tier]` | `[เลขวัน] [ตัวอักษรเกรด]` / `[%]` |
| ตัวอักษรเกรดในตาราง | **ไม่มีเลยทั้งจอ** | มีทุกช่อง |
| 干支 | ทุกช่อง | **ไม่มีสักช่อง** |
| legend | `≥60% วันดี · 40–59% · <40% ระวัง · วันพระ` (สี่เหลี่ยม 14px r5 · วันพระ = **ขาว+เส้นขอบ**) | จุดกลม + "วันนี้" |
| การ์ด | `r20 · p16 · gap14 · shadow 0 4 14 rgba(26,38,77,.06)` | r16 · p12 |
| แถบเลือกวัน | `[วันนี้ 84]` `[เดือน ▾ 150]` `[ปี (พ.ศ.) ▾ 110]` h62 | `วันนี้ + ‹ › + "กรกฎาคม · 2569"` |

🔑 **ตัวอักษรเกรดนั่งอยู่ในช่องที่ Figma วาง 干支 เป๊ะ** และ `CalendarDay.ganzhi` มีมาตั้งแต่ Phase 0 ของ goo แต่**ไม่เคยถูกเรนเดอร์**
⇒ ด่านที่ถามว่า "ตารางดูโอเคไหม" ผ่านฉลุย เพราะ*มีของ*อยู่ในช่องนั้น — ต้องถามว่า "ของในช่องนั้นเป็น 干支 หรือเปล่า" ถึงจะจับได้

## สีที่ไม่ต้องแก้เลย (ข่าวดี)
`#E2F4F6/#0B7A8C · #FEF1E0/#B47E35 · #FEE7E4/#CD3D2E · marker #9D85DA` ที่ node คืนมา **ตรงกับ `DAY_CELL_COLORS` + `CALENDAR_MARKER` ที่มีอยู่แล้วทุกค่า** ⇒ ระบบสีถูกมาตลอด ที่เพี้ยนคือ markup

## เรื่องที่ Figma บอกแล้วเราทำเป็นตรงข้าม
`368:9929` (วันที่ 14) มี**ทั้ง**พื้น sapphire **และ**เส้นขอบ `#9D85DA` พร้อมกัน
⇒ "วันนี้" กับ "วันพระ" เป็น **ข้อเท็จจริงคนละเรื่องของวันเดียวกัน** ต้องแสดงพร้อมกันได้
ของเดิมทำเป็น exclusive (เลือกแล้ว marker หาย) ⇒ ผู้ใช้ที่เปิดปฏิทินตรงวันพระพอดี **จะไม่เห็นว่าวันนั้นเป็นวันพระ**

## แถบเลือกวัน — ทำไมเป็น bottom sheet ไม่ใช่ `<select>`
บน iOS `<select>` เปิด picker ของระบบ ซึ่งขึ้นชื่อเดือนตามภาษา OS และปีเป็น **ค.ศ.** — จอนี้เป็น **พ.ศ.**
picker ที่โชว์ 2026 ข้างหน้าจอที่เขียน 2569 แย่กว่าไม่มี picker ⇒ sheet เป็นปุ่มธรรมดา 12 ปุ่ม
ของเดิมเดินได้ทีละเดือน ⇒ ไปอีก 6 เดือนต้องกด 6 ครั้ง และ**เปลี่ยนปีไม่ได้เลย**

## ไม่แตะ seam ของ goo
`useCalendarMonth` คง signature เดิมเป๊ะ (`goPrev/goNext/goToday`) — การกระโดดเดือนทำโดย**เดิน cursor ของเขา** N ครั้ง ไม่ได้เพิ่ม method ในไฟล์เขา

## Run command
```bash
set -a; . testenv/env/fe.env; set +a
./node_modules/.bin/next dev -p 3099
CAPTURE_HOST=http://localhost:3099 npx tsx harness/run-calendar-fidelity.ts   # 17/17
```

## proof-of-teeth (`run-calendar-fidelity.ts` → ✅ 17/17)
| 🦷 | ทำอะไร | ผล |
|---|---|---|
| `mut-grade-letter-back` | เอาตัวอักษรเกรดกลับไปใส่ช่อง 干支 (= สภาพ main) | **CAUGHT** |
| `mut-tier-swap` | ทาช่อง ≥60 ด้วยสีของ <40 | **CAUGHT** — 1 wrong |
| `mut-wanphra-erased` | ให้การเลือกลบ marker (= พฤติกรรมเดิม) | **CAUGHT** |

**อ่านของจริง ไม่ใช่อ่าน props**: ทุก assert อ่าน `textContent` + `getComputedStyle` ของช่องจริง

## 🔧 ซ่อม anchor เก่า 2 ตัวที่แดงค้าง — ซ่อมเครื่องมือ ไม่ใช่ลดมาตรฐาน

**1. `run-calendar-month` — "0 sapphire-selected" คือ ⏰ ระเบิดเวลา**
เดือน mock เป็นค่าคงที่ (กรกฎาคม 2026) แต่ด่าน assert ว่า "ต้องมีช่องวันนี้ 1 ช่องเสมอ" ⇒ **เขียววันที่เขียน แล้วแดงทุกวันหลังเดือนนั้นผ่านไป** — มันแดงเพราะปฏิทินเดินหน้า ไม่ใช่เพราะของพัง
→ เปลี่ยนเป็น: ถ้าวันนี้อยู่ในตาราง ต้องมี **พอดี 1** ช่อง · ถ้าไม่อยู่ ต้องมี **0** (เข้มขึ้นด้วยซ้ำ ไม่ได้ผ่อน)
→ อีกจุด: มันมองหา marker จาก `boxShadow` ซึ่งเป็น**กลไกเก่า** ตอนนี้ Figma ใช้ border จริง ⇒ อ่านทั้งสองทาง **ที่มันถูกวาด ไม่ใช่ที่มันเคยถูกเขียน**

**2. `run-calendar-day` — แดงเพราะ #166 ของผมเอง**
กฎ "0 running animations" เขียนตอนไม่มีอะไรขยับ · #166 ทำให้มาสคอต Mate AI ลอยอยู่ **ทุกหน้า** (motion ของ Figma เอง)
→ เจตนาของกฎคือ "**เนื้อหา**ต้องไม่ขยับตอนอยู่นอกจอ" ⇒ นับโดย**ยกเว้น nav** ซึ่ง `position: fixed` อยู่ในจอเสมอ และหยุดเมื่อ `prefers-reduced-motion` (มี `run-mateai-button` เฝ้าอยู่แล้ว)
→ **ผมเป็นคนทำให้มันแดง ผมซ่อมเอง** ไม่ปล่อยให้เป็น "แดงบน main อยู่แล้ว"

⇒ ทั้งสองตัว **กลับมาเขียว** ในใบนี้

## Pixel proof
`before/after @393` ของ `/v2/calendar` — เห็นชัด: แถบ 3 การ์ด · 干支 ทุกช่อง · legend ใหม่พร้อมสี่เหลี่ยม วันพระ แบบเส้นขอบ

## ไม่ regress
`tsc` ✅ · `scripts/*.test.ts` 52/52 ✅ · `run-app-header` ✅ · `run-calendar-day` ✅ (จากแดง) · `run-calendar-month` ✅ (จากแดง) · `run-calendar-notifications` ✅ · build ✅

## 🚩 ตามจริง — ที่ยังไม่ทำในใบนี้ (เจตนา ไม่ใช่ลืม)
- **การ์ด "วันนี้" ยังเป็นตัวเล็กแบบเดิม** — ของจริง (375:11100) เป็น superset ของการ์ด home ⇒ การสกัดออกมาใช้ร่วมกัน **ทำให้พิกเซลของ home ขยับ** จึงไปอยู่ใบหน้า (Zone 4) แทนที่จะแอบขี่มากับ refactor ตาราง
- **การ์ดฟ้า promo ยังไม่มี** — มันเป็นของ free เท่านั้น ต้องรอ tier มาถึง shell (Zone 4)
- **ยังกิน mock 100%** — endpoint ของ goo (#165) พร้อมแล้ว แต่ทางฝั่ง paid ต้อง `curl` ผ่าน staging ด้วย user ที่จ่ายจริงก่อน ตามที่ตกลงกับ goo (**A2**)
- ปุ่ม `วันนี้` ยังไม่ทำอะไรเมื่อวันนี้ไม่ได้อยู่ในเดือนที่ mock ครอบ — เป็นข้อจำกัดของ mock ไม่ใช่ของ UI

## 🔴 rule compliance
ไม่แตะ prod · ไม่มี secret · ไม่ force-push · PR เข้า main ไม่ self-merge · ไม่แก้ signature ของ hook ที่ goo เป็นเจ้าของ

## adversary sign-off
**พยายามหักล้างงานตัวเองด้วยอะไร**:
1. เอาตัวอักษรเกรดกลับไปใส่ช่อง 干支 → ด่านต้องแดง (นี่คือสภาพ main เป๊ะ) → แดง ✓
2. เชื่อ `data-*` attribute แทนที่จะอ่านของจริงไหม → ไม่ ทุก assert อ่าน `textContent` + `getComputedStyle`
3. เช็คว่าปุ่มเลือกเดือน "มีอยู่" พอไหม → ไม่พอ **กดจริงแล้วต้องเห็นตารางเปลี่ยนเดือน/ปี**
4. ปักความหนา border 1.6px ไหม → **ไม่** เพราะ Chrome ปัดเป็น device pixel (1.6→1 ที่ dsf 1) ⇒ ปักสี ซึ่งเป็น invariant จริง ไม่ใช่ตัวเลขที่ขึ้นกับจอ

**ตู๋ / goo — ช่วยยิงตรงนี้**: (ก) `goTo` เดิน cursor ทีละก้าว ถ้าผู้ใช้กระโดด 60 เดือนจะมี re-render ซ้อนจนสะดุดไหม (React batch แล้ว แต่ผมวัดแค่ ±5 ปีในด่าน) (ข) `data-selected`/`data-wanphra` ที่ผมใส่ มีทางไหนที่ค่ามันจริงแต่พิกเซลไม่จริง (หรือกลับกัน)
