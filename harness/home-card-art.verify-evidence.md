# ภาพจริง 2 จุดในหน้า home (ทาง ค ของฟีม)

**มุน 2026-08-06** · branch `feat/home-card-art` · base `5121aa9`

## ภาพ route จริง — ก่อน/หลัง ครบ 3 ความกว้าง
`harness/out/before-card{0,1}-{393,360,320}.png` · `after-card{0,1}-{393,360,320}.png`

## ผลดิบ
```
                 ก่อน                        หลัง
@393 card0  h190 · กรอบเปล่า            h169 · ภาพหนังสือ · มาสคอตครบ
@393 card1  h190 · กรอบเปล่า            h190 · ภาพปาจื่อ · ไม่มีมาสคอตการ์ด
overflow    ไม่มีทุกความกว้าง            ไม่มีทุกความกว้าง
scroll-x    false                       false

ด่านเดิมที่คุมการ์ดนี้ (รันจริง หลังแก้)
  run-zone4-motion  🟢 PASSED  ฟัน 3 ซี่ CAUGHT ครบ
  run-zone6-pajeu   🟢 PASSED  ฟัน 2 ซี่ CAUGHT ครบ
  run-zone4-sian    🟢 PASSED   ·  run-zone5-sinse 🟢 PASSED
scripts ทั้งหมด ✅   tsc ✅   CI-parity build ✅
```

## 📏 ตัวเลขที่ตู๋ขอ (จะได้ไม่ต้องวัดซ้ำ)

**1 · alpha bbox ของเนื้อจริง** (ไม่ใช่ intrinsic size)
```
ภาพหนังสือ  canvas 536x578  ink 533x555 (95.5%)  padding L0 T12 R3 B11
เรียนปาจื่อ  canvas 569x436  ink 545x431 (94.7%)  padding L12 T2 R12 B3
```
⇒ **ผมเตือนเองว่าขอบใสอาจทำให้ภาพดูเล็กกว่ากล่อง — วัดแล้วไม่จริง** ไม่ต้องชดเชย

**2 · WebP เทียบ PNG ที่ขนาดเรนเดอร์จริง (112px) ไม่ใช่ที่ 100%**
```
card0  max Δ65 · pixels moved >8 = 0.88% · mean Δ0.42/255
card1  max Δ31 · pixels moved >8 = 0.42% · mean Δ0.31/255
ขนาด   641 KB → 107 KB  (−83%)   59 KB + 48 KB
```
⇒ ค่าเฉลี่ยต่ำกว่าที่ตาเห็น · ที่ Δ สูงอยู่บนขอบคมชัด <1% ของพิกเซล · **และผมเปิดดูภาพจริงแล้วด้วย ไม่ตัดสินจาก KB**

**3 · ตำแหน่งมาสคอตใบไม้เทียบกล่องหนังสือ** (เมตริกเดียวกับที่ตู๋วัด before)
```
ก่อน (ตู๋วัด)  @393 101.1%  @360 91.7%  @320 80.3%   ⇒ เลื่อน 20.8 จุด · ที่ 393 หลุดออกนอกหนังสือแล้ว
หลัง          @393  80.1%  @360 80.1%  @320 80.1%   ⇒ เลื่อน 0 จุด
แนวตั้ง หลัง   y 9.9% ทุกความกว้าง
```

## proof-of-teeth
**ไม่ได้เพิ่มด่านใหม่** ตามเกณฑ์ — งานนี้ตาคนจับได้ · แต่ **ด่านเดิม 2 ตัวเข้ารหัสดีไซน์เก่าไว้ จึงแดง** และซ่อมให้ถามคำถามเดิมกับดีไซน์ใหม่:

| ด่าน | เดิมถาม | ตอนนี้ถาม | ฟัน |
|---|---|---|---|
| `run-zone6-pajeu` asset | "มาสคอต 2 ตัวเรนเดอร์ไหม" (นับ ≥2) | "**ทุกภาพในโซนเรนเดอร์จริงไหม**" (≥1 ไม่มีเจ๊ง) | คลาสบั๊กเดิม: asset 404 เงียบ |
| `run-zone6-pajeu` motion | "cohort 3 ชิ้นวิ่งไหม" | "**ทุกชิ้นที่แอนิเมตในโซนนี้วิ่งจริงไหม**" | reuse ตายเงียบ |
| `run-zone4-motion` slot | ผูกกับ `.hc-frame` **แข็ง** | `.hc-frame, .hc-float` ตามที่มีจริง | `mut-desync` กลับมากัด |
| `run-zone4-motion` rest | "slot ต้องคงมุมเอียง" | **ถามตามชนิด**: ตัวที่มีมุมต้องคงมุม · ตัวที่ลอยอย่างเดียวต้องราบ | `mut-reduce-kills-transform` ยังกัด |

🔴 **และ 2 ตัวนี้ crash ไม่ใช่ fail** — `querySelector('.hc-frame')!` โยน exception พอ Zone 4 มีภาพ
⇒ **ด่านที่ crash แย่กว่าด่านที่แดง เพราะมันไม่รายงานอะไรเลย** ⇒ ซ่อมให้ล้มแบบมีคำตอบ

ANCHOR: harness/run-zone4-motion.ts#mut-reduce-kills-transform

## adversary sign-off

1. **🔴 ผมทำ overflow ขึ้นมาเอง แล้วจับได้จากการเทียบ ก่อน/หลัง** — ตั้งภาพปาจื่อ 134px ⇒ @320 หัวข้อแตก (ต้องการ 94 ได้ 82) · **ภาพ "ก่อน" ไม่แตกที่ความกว้างไหนเลย ⇒ ของผม** ⇒ ลดเหลือ 112 + จำกัดที่ 38% ของการ์ด
2. **🔴 CTA ตก 2 บรรทัด — จับได้จากภาพ "ก่อน" เหมือนกัน** ("ดูรายละเอียดเพิ่มเติม" เคยอยู่บรรทัดเดียว) ⇒ คืนความกว้าง 175px ให้ข้อความ
3. **re-anchor ไม่ใช่ค่าใหม่** — ตู๋เตือนว่าผูก `bottom` กับขอบการ์ดจะเลื่อนอีกทุกครั้งที่ความสูงเปลี่ยน ⇒ ย้ายไปผูกกับ **กล่องภาพ** และ**ขนาดมาสคอตก็เป็น % ของภาพ** ⇒ drift 20.8 → 0
4. **`run-zone3-somphong` timeout — ตรวจแล้วว่าไม่ใช่ของผม** รัน stash ของตัวเองออกแล้ว **มัน timeout บน main สะอาดเหมือนกัน** ⇒ ของเดิม **แต่ยังไม่มีใครรู้** (ขอส่งเป็นหนี้)
5. **ยังไม่ครอบ (A2)** — ไม่ได้วัด CLS เป็นตัวเลข (ตรวจว่าไม่มี scroll-x และภาพมี `width`/`height` + `aspect-ratio` จึงจองที่ตั้งแต่ก่อนโหลด) · ไม่ได้ทดสอบบนเครื่องช้าจริง · ไม่ได้ตรวจ `/v2` ตัวจริง (วัดที่ `home-preview` = คอมโพเนนต์เดียวกัน แต่ไม่ใช่เส้นทางจริง — เหมือนที่ตู๋กำกับไว้เอง)
6. **ไฟล์ PNG ต้นฉบับ ❌ ไม่ commit** — ใบนี้ใช้ WebP ⇒ PNG 641 KB จะเป็นไฟล์ที่ไม่มีใครใช้ใน `/public` (และเป็นของที่ถูกอ้างผิดได้ในอนาคต) ⇒ **2 ไฟล์ที่ค้าง staged ในโคลนหลักควร `git restore --staged`** โดยเจ้าของ — ผมไม่แตะ index ของคนอื่น
