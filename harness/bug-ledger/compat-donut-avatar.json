{
  "id": "compat-donut-avatar",
  "description": "มุน 2026-08-03 (ฟีม, Figma 636:19230 + 636:19061): the ดวงสมพงศ์ hero donut + avatar must match Figma's OPEN ring, not the old filled-lime 'coin'. Colour, sampled from the node (not guessed): hero card = Sapphire #1455A4 (was #0B305B navy — too dark, the core 'เรื่องสี'); ring track = #083C7B; progress = lime #E1FF00; grade = lime; % = WHITE. The centre lime DISC is removed. ScoreRing is SHARED by 5 surfaces (compat hero · calendar DayScoreCard · v2-home · home-fortune) — calendar+home sit on LIGHT pastel bg where white % would VANISH, so the open-ring text is bg-adaptive via `onDark`: dark card → lime grade + white %; light card (default) → navy grade + navy % (legible). Proven: ring rendered before/after on all 3 bgs (navy/pastel/white-cyan) — light-bg text stays navy, no white-on-light. Avatar: when the form carries no imageProfile the slot is no longer empty (ฟีม) — a Mumate-logo teal-fill FALLBACK fills it (object-cover of /images/mumate/ic_logo_app.svg, lime ring). This is a BRANDED placeholder, NOT fabricated user data, so rule 4 still holds: `-photo` stays absent (mut-fake-photo trips on a fabricated real photo); `-avatar-fallback` is the legit stand-in and is positively asserted. gate_layer=visual (rendered pixels vs Figma node + computed text-colour + geometry). Pixel-proven @393 on the real route (fe proxy→bazi prod→scenic image): sapphire card + open donut + teal fallback. Did NOT touch pages/matching or constants/api/api-user-matching (git diff 0). ⚠️ enforced_by REMOVED 2026-08-07 (goo · harness-tidy ใบ ข · on บอง’s ruling): the anchor named here was MOVED to harness/archive/ — nothing runs it (not CI, not an npm script), so the field was advertising a guard that does not run (the shape ตู๋ caught in #189/#191). Rule: connect it or withdraw the claim, never leave the claim standing. The anchor FILE is kept in harness/archive/ (Rule 1 · Nothing is Deleted); to revive, wire it into CI first, then re-add enforced_by. WAS: harness/run-compat-3c.ts#avatar-fallback"
}
