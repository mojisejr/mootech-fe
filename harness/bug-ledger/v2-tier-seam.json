{
  "id": "v2-tier-seam",
  "description": "goo 2026-08-03 (μุน Zone-4 dispatch): page-agnostic paid-tier seam useV2Tier(): {isPaid: boolean|null, loading}. BUG-CLASS THIS ANCHOR OWNS — a paid-gate that GUESSES an unknown tier: unknown is wrong BOTH ways (read-as-free hides a paying user's content; read-as-paid leaks paid content to free), so computeTier returns null (NOT false) while loading AND on fetch-error, and NEVER returns isPaid=true without strict payment.is_not_expired===true. Pure lib/v2/tier.ts (isPaidMember = the SINGLE paid rule + computeTier = full state reducer); lib/home/profile.ts refactored so showUpgrade = !isPaidMember → header badge and tier gate share ONE rule, no drift (home-profile.test 7/7 unchanged). Hook features/auth/hooks/useV2Tier.ts = thin fetch (UserGetById, cookie cookie-mumate-id) → computeTier; idempotent effect (alive guard, no doneRef, mirrors useV2Home #176), NO routing/redirect (usable off-home). Teeth (cp-snapshot mutants, 16 assertions): M1 error→isPaid:false → RED · M2 loading→isPaid:false → RED · M3 isPaidMember truthy not ===true → RED. tsc0 · verify-architecture PASS. NOT covered: hook's browser fetch-lifecycle (StrictMode/alive/userId-reset) tsc-checked + mirrors proven useV2Home pattern but not rendered-tested — μุน exercises at Zone-4 wire (paid/free/loading pass) to close product loop; pure reducer fully covered. Did NOT touch useV2Home routing, μุน UI, or endpoints.",
  "enforced_by": "scripts/v2-tier.test.ts#v2-tier-gate-both-directions"
}
