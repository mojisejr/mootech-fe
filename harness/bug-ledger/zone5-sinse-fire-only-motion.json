{
  "id": "zone5-sinse-fire-only-motion",
  "description": "มุน 2026-07-28: Zone 5 ทักซินแส / section-mascot (Figma 333:6989) — a full-bleed sapphire banner: text + lime Secondary CTA, the big water-owl mascot overflowing bottom/right (STATIC), a small fire sprite (the ONLY animated element, loop 2000ms), and a cream white-mound wave. Phase-0 asset pairing resolved by IMAGE (not name/coords, the Zone-4 lesson): downloaded the big-mascot raw fills of node 333:6997 — BOTH raw images are the 水-owl mascot (z5-raw1 1.74MB byte-identical to the prior mascot-big.png export; z5-raw2 a lower-res copy of the same character), so the Zone 5 big mascot IS the Zone 4 mascot -> REUSE public/images/v2/zone4/mascot-sian.png, NO new export. (My first read of the CLIPPED 220px node thumbnail looked like a star-emblem different mascot — a misread the full-res source corrected; verify the asset, not the thumbnail.) The fire sprite (node 333:7023) is byte-different from every zone4 asset -> genuinely NEW: 1 file public/images/v2/zone5/sprite-fire.png (176x220, 43KB) — well under the 20-file/5MB tripwire. Bug-class the anchor owns (pixels lie): (1) fire-only-motion — EXACTLY the fire animates, the big mascot is STATIC (Figma นิ่ง); a mascot that accidentally animates or a fire that does not both pass tsc; (2) reduced-motion — the fire stops (getAnimations empty); its rest pose IS identity (rotate0 scale1) so resting at identity is CORRECT here (unlike hc-small/hc-frame there is no base transform to lose), a distinction the anchor encodes; (3) secondary-cta — the CTA is the lime-FILLED Secondary variant by computed style, not an outline; (4) full-bleed — the banner breaks out of the px-4 column to ~viewport width (measured 393@393); (5) no-overflow-x — the mascot overflows the CARD but is clipped, the page must not scroll @393/360/320. verify-the-instrument 2-way (fire reads running live AND empty under reduce). Teeth: mut-mascot-animates (give the static mascot an animation) and mut-fire-reduce-runs (re-enable fire under reduce), both CAUGHT. Rendered @393 matches Figma 333:6989 side-by-side. gate_layer=visual/motion. ⚠️ enforced_by REMOVED 2026-08-07 (goo · harness-tidy ใบ ข · on บอง’s ruling): the anchor named here was MOVED to harness/archive/ — nothing runs it (not CI, not an npm script), so the field was advertising a guard that does not run (the shape ตู๋ caught in #189/#191). Rule: connect it or withdraw the claim, never leave the claim standing. The anchor FILE is kept in harness/archive/ (Rule 1 · Nothing is Deleted); to revive, wire it into CI first, then re-add enforced_by. WAS: harness/run-zone5-sinse.ts#mut-mascot-animates"
}
