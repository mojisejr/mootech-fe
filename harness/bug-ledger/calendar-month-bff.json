{
  "id": "calendar-month-bff",
  "description": "goo 2026-08-03 (ฟีม ไฟเขียว · มุน dispatch): v2 ปฏิทินดวง BFF proxies — 2 routes + shared lib (lib/v2-calendar/month.ts). calendar-month (POST, PAID via resolveMembership: free/expired/unknown/error → allowed:false, NO bazi call) returns personalised per-day {date,dayOfMonth,dayGanzhi,overallPercent,wanPhra} from bazi man-vs-day month; almanac-month (GET, UNGATED) returns วันพระ overlay for BOTH tiers — the SINGLE วันพระ source (ฟีม answer C = bazi-computed), NOT chinese_calendar (untouched), so free/paid never disagree. BUG-CLASS THIS ANCHOR OWNS — วันพระ MISCATEGORISATION: bazi almanac specialDays[] mixes religious วันพระ (thai-buddhist·chinese-religious) with government holidays (category government) and secular festivals (festival-chinese). A naive 'any special day ⇒ วันพระ' flags Mother's Day (2026-08-12) as วันพระ; a single-category check MISSES วันพระ when it co-occurs with สารทจีน (2026-08-27). isWanPhraDay filters by an exact category Set; mergeCalendarMonth joins wanPhra BY DATE (not index — the two upstream day-arrays differ in length/order). Teeth (cp-snapshot mutants, 21 assertions): mut-any-special-day → RED (gov flagged); mut-index-join → RED. Fixtures are REAL rows curl'd from bazi-sft-dataset.vercel.app 2026-08. VERIFIED REAL: man-vs-day month HTTP200 6.8s cold/3.7s warm days[]-shape; almanac 3.5s specialDays categories; almanac-month HANDLER end-to-end vs REAL bazi = 200/3.15s/31d, 08-12 wanPhra=false·08-13=true·08-27=true, cache 2nd=0ms. NOT curl'd through a booted server: paid calendar-month route (local next dev forces MAINTENANCE_MODE middleware → /maintenance; resolveMembership needs Supabase + paid row) — proven by parts (real man-vs-day curl + merge unit/mutant + gate usage-core 29). LATENCY: first month view ≈6.8s (upstream) — product cost flagged, per-(user,month) cache makes re-view instant, bazi-side optimize proposed separately. Did NOT touch chinese-calendar/month.ts, μุน's calendar.tsx/grade-colors.ts, or any sibling (git diff origin/main = new files only).",
  "enforced_by": "scripts/calendar-month.test.ts#calendar-month-wanphra-category"
}
