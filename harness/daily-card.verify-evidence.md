# EYE PROOF — การ์ด "ดวงวันนี้" ใช้ร่วมกัน home ↔ ปฏิทินดวง

**Anchor:** `harness/run-daily-card.ts` (12/12)
**PR:** feat/v2-daily-card · base = main (`3d46424`)
**Ledger:** `harness/bug-ledger.json` → `daily-fortune-card`
**Plan:** ❄️ FROZEN FINAL — `lamun-oracle/ψ/plans/2026-08-03_FROZEN-calendar-arc-zones-1-3.md` (Zone 3.3)

ANCHOR: harness/run-daily-card.ts#mut-home-gradient-on-calendar

## ที่มา
ฟีม: *"เอาอันนี้จากหน้า home ของ user มาใส่ด้วย … มันมีอยู่ในหน้าปฏิทินดวง"*
`get_design_context` ที่ `375:11100` ยืนยัน: **การ์ดของปฏิทินคือ superset ของการ์ด home** — วงแหวน 90px เท่ากัน หัวข้อเหมือนกัน 2 คอลัมน์เหมือนกัน · ที่เพิ่มมาคือ chip 干支 · แถววันพระ · 2 บรรทัดต่อฝั่ง · ปุ่มในการ์ด

⇒ ทำเป็น **component เดียว 2 variant** ไม่ใช่ก๊อปการ์ดที่มีอยู่แล้วอีกใบ

## 🔒 หลักฐานว่า home ไม่ขยับ — วัดจริง ไม่ใช่ตั้งใจ
```
HOME pixel diff: 0 of 597360 = 0.000%
```
เทียบ before/after ของ `/v2/home-preview` @393 dsf2 ทุกพิกเซล — **ศูนย์**
(การสกัด component ที่ shipped แล้ว ต้องมี pixel-diff เสมอ ไม่ใช่แค่ tsc เขียว — บทเรียนจาก ScoreRing)

## ปฏิทินได้ของ Figma
| | ก่อน | หลัง |
|---|---|---|
| การ์ด | เล็ก r16 + ปุ่ม**แยกอยู่ข้างล่าง** | `r28 · p24` ขาว + **ปุ่มอยู่ในการ์ด** (Figma `375:11981`) |
| วงแหวน | เส้นบาง สีตามเกรด | **แผ่นไลม์** + เลข sapphire + arc sapphire |
| หัวข้อ | ประโยคฮาร์ดโค้ด `"วันดวงดีมาก แค่เริ่มก็สำเร็จแล้ว"` เหมือนกันทุกวัน | `detail.summary` ของวันนั้นจริง |
| เนื้อหา | ไม่มี | chip 干支 · เหมาะกับวันนี้/ควรเลี่ยง ฝั่งละ 2 บรรทัด |

🔑 ของเดิม**เขียนคำชมไว้ตายตัว** — วันคะแนน 21% ก็ยังขึ้นว่า "ดวงดีมาก"

## ทำไมไม่ยก home ไปเป็นแบบ Figma ด้วย
มันคือการเปลี่ยนหน้าที่ ฟีม เซ็นไปแล้ว **ด้วยความคิดผมเอง ในใบที่หัวข้อคือปฏิทิน**
การ์ดสองใบไม่ตรงกันจริง (พื้น gradient vs ขาว · arc ตาม verdict vs แผ่นไลม์) — **เป็นคำถามของ ฟีม ไม่ใช่ของผม** ⇒ บันทึกไว้ ไม่ตัดสินเงียบๆ

## proof-of-teeth (12/12)
| 🦷 | ผล |
|---|---|
| `mut-home-gradient-on-calendar` — ให้พื้นของ home รั่วมาที่ปฏิทิน | **CAUGHT** |
| `mut-ring-not-lime` — เอาแผ่นไลม์ออก | **CAUGHT** |
| `mut-cta-outside` — ย้ายปุ่มออกนอกการ์ด (= สภาพเดิม) | **CAUGHT** |

ด่านเช็ค **2 ทาง**: home ต้องไม่มี chip 干支 · ปฏิทินต้องไม่มี gradient — component ที่ share กันพังได้ทั้งสองทิศ

## ไม่ regress
`tsc` ✅ · tests 52/52 ✅ · `run-fortune-fidelity` ✅ (data-binding + clamp 150% + empty-facet ยังครบ) · `run-calendar-fidelity` ✅ · `run-app-header` ✅ · build ✅

## 🚩 ตามจริง
- **"สะสม · 財 ทิศ W" กับ "แรม ๑๕ ค่ำ เดือน ๘"** ยังไม่ขึ้น — goo ยืนยันว่า `/api/home` ไม่มี field พวกนี้ตรงๆ และ mock ก็ไม่มี ⇒ chip 干支 ขึ้นแล้ว แต่ข้อความข้างๆ ยังว่าง (**A2** ไม่ใส่ของปลอม)
- บรรทัดวันที่ยังไม่มีชื่อวัน ("อังคารที่") เพราะ `formatThaiLongDate` ไม่คืนวันในสัปดาห์ (**A2** เล็ก)
- ยังกิน mock 100% — endpoint ของ goo พร้อมแล้วแต่ paid ต้อง curl staging ก่อน

## 🔴 rule compliance
ไม่แตะ prod · ไม่มี secret · ไม่ force-push · PR เข้า main ไม่ self-merge · home ไม่ขยับสักพิกเซล

## adversary sign-off
1. เชื่อว่า "สกัดแล้วเหมือนเดิม" เพราะ tsc เขียวไหม → **ไม่** วัด pixel-diff ได้ 0/597360
2. เช็คแค่ฝั่งปฏิทินพอไหม → ไม่พอ ด่านเช็คว่า home **ไม่**ได้ของฝั่งปฏิทินมาด้วย
3. `run-fortune-fidelity` (ด่านของ ตู๋/goo ที่เฝ้า data-binding) ยังเขียวไหมหลังสกัด → เขียว teeth ทั้ง 3 ยังกัด

**ตู๋ / goo — ช่วยยิง**: (ก) variant prop แบบนี้จะพาไปสู่ "การ์ดเดียวรับ 5 หน้าจนอ่านไม่ออก" ไหม ควรตัดตรงไหน (ข) `detail.summary` ของ mock อาจยาวกว่าของจริง — มีเคสไหนที่หัวข้อดันปุ่มตกจอ
