# mootech-fe — LOCAL test-env. COMMITTABLE (no real secrets).
# stack.sh copies this → mootech-fe/.env (backs up existing first).
NODE_ENV=development                # so pages/dev-login.tsx is enabled (gated NODE_ENV!==production)
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/mumate_test   # drizzle ssl:'require' → SSL postgres
NEXT_PUBLIC_BACKEND_URL=http://localhost:4000    # mootech-be (User/ChineseHoroscope/Analytic/Mascot)
BAZI_BASE_URL=http://localhost:3100              # bazi (/api/home persona+fortune)
V2_PREVIEW_KEY=local-testenv        # team gate passkey (simple local value)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=local-testenv-nextauth-secret    # dummy — dev-login bypasses OAuth
